# force-app/main/default

Default Salesforce metadata folder used by this template.

Currently this folder contains only a placeholder file (`.gitkeep`) and is intended for project metadata as the template is customized.
